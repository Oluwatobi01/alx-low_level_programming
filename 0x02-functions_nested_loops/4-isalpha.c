#include <stdio.h>

/**
 * checks for alphabetic character.
 *  _isalpha(int c); Returns 1 if c is a letter, lowercase or uppercase
 *  Return 0 if otherwise
 */

int _isalpha(int c)
{
	if (c >= 'a' && c <= 'Z')
	{
		Return (1);
	}
	else
	{
		return (0);
	}
}
