#include <stdio.h>

/**
 * main - check if a character is a lower case
 * if YES returns 1 and if otherwise 0
 * islower - Entry point
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		Return (1);
	}
	else
	{
		Return (0);
	}
}
