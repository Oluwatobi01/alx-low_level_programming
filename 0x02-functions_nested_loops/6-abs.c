#include <stdio.h>

/**
 * function that computes the absolute value of an integer.
 * @n: the factor to be operated upon
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
